<% if @books.empty? %>
    <h1> Aun no ha creado cuadernos </h1>
<% else %>
    <h2> User books <%= current_user.email %> </h2>
<% end %>
<%= button_to 'Export all', export_all_book_path(:id => 0), method: :put %>
<%= link_to "New Book", new_book_path()%>
<ul>
    <li> 
        Global 
        <%= link_to 'List Notes', notes_path() %>
        <%= button_to 'Export', export_book_path(:id => 0), method: :put %>
    </li>
    <% @books.each do |book| %>
        <li> 
            Titile: <%= book.title %>
            <%= link_to 'List Notes', notes_path(:id_book => book.id) %>
            <%= link_to "Edit", edit_book_path(book.id)%>
            <%= button_to 'Export', export_book_path(:id => book.id), method: :put %>
            <%= button_to 'Delete', book, method: :delete, confirm: 'Seguro que desea eliminar el libro?'  %>
        </li>
    <% end %>
</ul>

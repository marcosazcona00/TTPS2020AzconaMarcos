<nav class="navbar navbar-expand-lg navbar-light bg-light">
        <% if user_signed_in? %>
            <a class="navbar-brand" href="#"> TTPS <span class="sr-only"><span></a>
            <%= link_to 'Books', books_path(), class: "nav-link" %>
            <a class="navbar-brand" href="#"> Loggued as <%= current_user.email%> </a>
            <%= button_to 'Sign Out',destroy_user_session_path(),  method: :delete, class: "btn btn-outline-secondary" %>
        <% end %>

</nav>